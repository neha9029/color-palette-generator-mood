function Header() {
  return (
    <header>
      <h1 className="title">Color Palette</h1>
      <p className="subtitle">
        Select a mood to generate matching color palette
      </p>
    </header>
  );
}

export default Header;
